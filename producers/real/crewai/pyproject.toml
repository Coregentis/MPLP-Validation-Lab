[project]
name = "crewai-producer"
version = "2.0.0"
description = "CrewAI substrate evidence producer"
requires-python = ">=3.11"
dependencies = [
    "crewai==0.28.8",
    "setuptools",
    "langchain",
]

[tool.uv]
managed = true
